---
name: GitHub credentials — LWM-Azure
description: GitHub repo info for the LWM-Azure repo
metadata:
  type: reference
---

**GitHub username:** Iam-mithran (git user: LearnWithMithran)
**Repo:** https://github.com/Iam-mithran/LWM-Azure.git

**PAT:** stored in `C:\Users\TheUsualGenius\.git-credentials` (never commit the token value here)
**Credential helper:** `git config --global credential.helper store` — pushes work automatically with no prompts.

To push: `git push origin main` from the repo directory — no further auth needed.
